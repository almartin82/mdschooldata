# Get Maryland LSS (Local School System) codes

Returns a mapping of LSS numbers to names for Maryland's 24 school
systems.

## Usage

``` r
get_lss_codes()
```

## Value

Named vector with LSS codes as names and LSS names as values
