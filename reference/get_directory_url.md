# Get the download URL for directory type

Constructs the download URL for Maryland school directory.

## Usage

``` r
get_directory_url(directory_type = "all")
```

## Arguments

- directory_type:

  Type of directory ("charter_schools", "all")

## Value

Character string with download URL or named list of URLs
